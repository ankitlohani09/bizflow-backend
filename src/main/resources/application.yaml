spring:
  application:
    name: bizflow-backend

  datasource:
    url: jdbc:mysql://localhost:3306/bizflow_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  mail:
    host: mail.thinkai.in
    port: 465
    username: onboarding.pos@think-ai.in
    password: "3d2D+B#{*Ge!"
    protocol: smtp
    properties:
      mail.smtp.auth: true
      mail.smtp.ssl.enable: true
      mail.smtp.ssl.trust: mail.thinkai.in
      mail.smtp.starttls.enable: false

server:
  port: 8080
  address: 0.0.0.0
  servlet:
    context-path: /api/v1

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000  # 24 hours in ms

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

logging:
  level:
    com.bizflow: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: INFO

# Storage Configuration
storage:
  type: local # change to 'azure' for cloud storage

# Azure/Cloud Settings (Used if storage.type is 'azure')
AWS_CONNECTION_STRING: ${AWS_CONNECTION_STRING:}
AWS_CONTAINER_NAME: ${AWS_CONTAINER_NAME:}